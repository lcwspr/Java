# 位运算符
## 位运算符基本用法
```
    & 按位与，有0则0，全1为1
    | 按位或，有1则1，全0为0
    ^ 按位异或，相同为0，不同为1
    ~ 按位取反，翻转操作数的每一位
    << 左移，右边补符号位
    <<< 无符号右移
```

## 使用位异或实现2个数交换的讲解
```
int x = 10;
int y = 20;
x = x ^ y;
y = x ^ y;
x = x ^ y;
```

## 最有效率的算出2 * 8的结果
```
sout(2 << 3)
```